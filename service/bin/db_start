#!/bin/bash

# 当前文件路径
SH_PATH=$(cd "$(dirname "$0")"; pwd)

# 端口
PORT=7000
# 数据库
DB_PATH=${SH_PATH}/../../data
# 日志
LOG_PATH=${SH_PATH}/../../data/db_log/log

echo "端口号="${PORT}
echo "数据库="${DB_PATH}
echo "日志="${LOG_PATH}

#开启服务
mongod --port ${PORT} --dbpath ${DB_PATH} --logpath ${LOG_PATH}